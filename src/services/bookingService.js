// import { getTokenFromLocalStorage } from '../utils/functionUtils'
// import { customAxios }  from '../utils/apiUtils';

// const getAuthHeaders = () => {
//   const token = getTokenFromLocalStorage();
//   // if (!token) throw new Error('No token found. Please login.');
//   return { Authorization: `Bearer ${token}` };
// };

// const handleError = (error, fallbackMessage = 'Something went wrong') => {
//   // console.error('API Error:', error); // Optional: can be removed in production
//   const message = error?.response?.data?.message || fallbackMessage;
//   throw new Error(message);
// };

const bookingServices = {
}

export default bookingServices
